# Required
DOMAIN=jhub.example.com
LETSENCRYPT_EMAIL=admin@example.com

# GitHub OAuth App credentials
GITHUB_CLIENT_ID=your_github_oauth_client_id
GITHUB_CLIENT_SECRET=your_github_oauth_client_secret

# Callback must match your GitHub OAuth App settings
OAUTH_CALLBACK_URL=https://${DOMAIN}/hub/oauth_callback

# Hub base path (normally "/")
HUB_BASE_URL=/

# Org-based access control (preferred):
# Comma-separated GitHub orgs or org:team entries.
# Example: my-org, my-org:platform-team
ALLOWED_ORGS=

# Optional: explicit usernames allowed (comma-separated)
# If ALLOWED_ORGS is set, this acts as an additional restriction.
ALLOWED_USERS=

# Optional admins (subset of allowed users)
ADMIN_USERS=username1

# Single-user notebook image (choose one):
# - Custom Python 3.11 (CPU): build from singleuser/Dockerfile and set below
# - Custom Python 3.11 (GPU): build from singleuser/Dockerfile.gpu and set below
# - Or use a prebuilt image of your choice that includes jupyterhub-singleuser
# Recommended for Python 3.11 (CPU): SINGLEUSER_IMAGE=singleuser:py311
# Recommended for Python 3.11 (GPU): SINGLEUSER_IMAGE=singleuser:py311-cuda
SINGLEUSER_IMAGE=jupyter/datascience-notebook:latest

# Path on the host to mount into user containers for large models/data
HOST_WORKSPACE=/workspace

# Subdirectory under HOST_WORKSPACE that will be mounted into each
# user’s home as ~/Shared for easy collaboration
SHARED_DIR=shared

# Enable GPU access for user containers (requires NVIDIA Container Toolkit)
ENABLE_GPU=true
